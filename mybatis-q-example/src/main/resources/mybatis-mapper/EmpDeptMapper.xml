<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.github.mybatisq.mapper.EmpDeptMapper">
    <select id="count" parameterType="com.github.mybatisq.Query" resultType="java.lang.Integer">
        select count(0) <include refid="com.github.mybatisq.mapper.QMapper.countFrom"/>
    </select>

    <select id="select" parameterType="com.github.mybatisq.Query" resultType="com.github.mybatisq.entity.EmpDept">
        select ${tableAlias}.edid,${tableAlias}.empId,${tableAlias}.deptId <include refid="com.github.mybatisq.mapper.QMapper.selectFrom"/>
    </select>

    <insert id="insert" parameterType="com.github.mybatisq.entity.EmpDept" useGeneratedKeys="true" keyProperty="edid">
        <trim prefix="insert empDept (" suffix=")" suffixOverrides=",">
            <if test="edid != null">edid,</if>
            <if test="empId != null">empId,</if>
            <if test="deptId != null">deptId,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="edid != null">#{edid},</if>
            <if test="empId != null">#{empId},</if>
            <if test="deptId != null">#{deptId},</if>
        </trim>
    </insert>

    <update id="update" parameterType="com.github.mybatisq.entity.EmpDept">
        <trim prefix="update empDept set" suffix="where edid=#{edid}" suffixOverrides=",">
            <if test="empId != null">empId=#{empId},</if>
            <if test="deptId != null">deptId=#{deptId},</if>
        </trim>
    </update>

    <delete id="delete">
        delete from empDept where edid=#{edid}
    </delete>
</mapper>